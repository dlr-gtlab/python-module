${JOBNAME}:
  stage: deploy
  rules:
    - if: $UNSTABLE_BRANCH == null && $CI_COMMIT_TAG != null
  script:
    - echo "Hello template"
    - echo $PLATFORMNAME
    - echo $RELEASESTATUS
    - echo $COMMIT_BRANCH
    - echo $COMMIT_BRANCH
  tags:
    - Win10
  variables:
    PLATFORMNAME:  ${PLATFORMNAME}
    RELEASESTATUS: ${RELEASESTATUS}